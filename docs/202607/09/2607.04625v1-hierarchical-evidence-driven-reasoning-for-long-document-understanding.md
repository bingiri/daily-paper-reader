---
title: Hierarchical Evidence-Driven Reasoning for Long Document Understanding
title_zh: 层次化证据驱动推理用于长文档理解
authors: "Junyu Xiong, Yonghui Wang, Rongjian Gu, Chenyu Liu, Bing Yin, Wengang Zhou, Houqiang Li"
date: 2026-07-06
pdf: "https://arxiv.org/pdf/2607.04625v1"
tags: ["query:mm-reasoning"]
score: 8.0
evidence: 层次化证据驱动的多模态RAG推理
tldr: "现有多模态RAG存在检索干扰和单次传递易遗漏核心证据的问题。本文提出HIEVI-RAG，采用层次化证据驱动的四阶段框架，通过问题分解、粗粒度检索、细粒度验证和记忆引导迭代生成，实现长文档理解。在四个基准上平均准确率超过最强基线8.05%。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.04625v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1642, \"height\": 1074, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.04625v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1660, \"height\": 402, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.04625v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1635, \"height\": 581, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.04625v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 840, \"height\": 216, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.04625v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1662, \"height\": 499, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.04625v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1662, \"height\": 279, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.04625v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1657, \"height\": 307, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.04625v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1658, \"height\": 483, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.04625v1/table-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 826, \"height\": 343, \"label\": \"Table\"}]"
motivation: 现有多模态RAG面临检索干扰和单次传递依赖初始检索成功导致级联错误的问题。
method: 提出HIEVI-RAG，包含层次化问题分解、粗粒度页面检索、基于EVIAGENT的细粒度验证和记忆引导迭代生成。
result: "在四个基准上平均准确率超过最强基线8.05%，显著优于现有开源方法。"
conclusion: 层次化证据驱动框架有效解决了多模态RAG的关键缺陷，提升了长文档理解的鲁棒性。
---

## 摘要
检索增强生成（RAG）通过利用检索机制将输入图像限制为高度精选的子集，简化了长文档理解。然而，现有的多模态RAG流程主要面临两个关键挑战：首先，标准语义相似性检索器经常检索到主题重叠但缺乏答案的干扰页面，从而误导下游生成；其次，僵硬的单次流程高度依赖初始检索成功，任何核心证据的遗漏都不可避免地导致级联错误。为解决这些挑战，我们提出了HIEVI-RAG，一个用于闭域文档理解的层次化、证据驱动的多模态RAG框架。HIEVI-RAG系统地将复杂查询分解为协作的四阶段流程：（1）层次化问题分解，将多跳根查询拆解为原子子问题；（2）粗粒度视觉页面检索，利用多模态检索器基于语义相似性获取候选页面；（3）通过EVIAGENT进行细粒度页面验证，EVIAGENT是一个专门的多页面验证器，使用GRPO训练以在多图像块上执行跨页面推理；（4）记忆引导的迭代生成，利用累积的子问题上下文在优先级序列上执行多轮动态推理。在四个基准上的广泛评估表明了我们框架的强大有效性和协同作用，其显著优于现有的开源基线，并且在准确率上平均超过最强报告的基线8.05%。

## Abstract
Retrieval-Augmented Generation (RAG) streamlines long-document understanding by leveraging retrieval mechanisms to restrict input images to a highly curated subset. However, existing multimodal RAG pipelines primarily face two critical challenges: first, standard semantic similarity retrievers frequently fetch topically overlapping yet answer-void distractor pages that mislead downstream generation; second, rigid single-pass pipelines heavily depend on initial retrieval success, where any omission of core evidence inevitably causes cascading errors. To address these challenges, we introduce HIEVI-RAG, a hierarchical, evidence-driven multimodal RAG framework for closed-domain document understanding. HIEVI-RAG systematically factorizes complex queries into a cooperative four-stage pipeline: (1) hierarchical question decomposition to break multi-hop root queries into atomic child questions; (2) coarse visual page retrieval leveraging a multimodal retriever to fetch candidate pages based on semantic similarity; (3) fine-grained page verification via EVIAGENT, a specialized multi-page verifier trained with GRPO to execute cross-page reasoning over multi-image blocks; and (4) memory-guided iterative generation that leverages accumulated sub-question context to execute multi-round, dynamic reasoning over the prioritized sequence. Extensive evaluations across four benchmarks demonstrate the robust efficacy and synergy of our framework, which significantly outperforms existing open-source baselines and exceeds the strongest reported baseline by an average of 8.05% in accuracy.

---

## 论文详细总结（自动生成）

# 论文详细中文总结

## 1. 论文的核心问题与整体含义（研究动机和背景）
- **核心问题**：现有多模态检索增强生成（RAG）在处理长文档理解时面临两大挑战：
  1. **检索干扰**：标准语义相似性检索器常返回主题相关但缺乏答案的干扰页面，误导下游生成。
  2. **单次传递依赖**：刚性单次流程高度依赖初始检索成功，一旦遗漏核心证据，会导致级联错误，无法正确生成答案。
- **应用背景**：闭域文档理解在科学、金融、企业知识管理等领域至关重要，但文档页数可达数百页，直接输入大语言模型会超出上下文窗口限制。RAG通过检索缩小输入范围，但现有方法仍存在上述缺陷。
- **整体含义**：论文旨在通过层次化、证据驱动的多模态RAG框架，系统性地解决检索不精准和单次传递易遗漏的问题，提升长文档问答的鲁棒性和准确性。

## 2. 论文提出的方法论：核心思想、关键技术细节、公式或算法流程
- **核心思想**：将复杂查询分解为原子子问题，通过“粗粒度检索→细粒度验证→记忆引导迭代生成”的协作流程，确保核心证据被准确捕获，并利用累积上下文消除信息遗漏。
- **关键技术细节**：
  - **层次化问题分解**：使用轻量级LLM（Qwen3.5-4B）将根问题分解为最多5个原子子问题，降低检索难度，保证分散证据的召回。
  - **粗粒度视觉页面检索**：采用Ops-ColQwen3-4B（ColPali风格）先离线编码所有页面为多向量表示，再在线将根问题和每个子问题编码，通过晚期交互计算相似度，为每个查询召回Top-K（K=20）候选页面。
  - **细粒度页面验证（EVIAGENT）**：使用基于GRPO训练的专用多页面验证器。训练时，模型输出结构化内容：推理轨迹（`<think>`）、证据决策（`<evidence_page>`，二进制序列T/F）、最终答案（`<answer>`）。奖励函数为加权联合奖励：`R = λ_fmt * r_fmt + λ_evi * r_evi + λ_acc * r_acc`，其中`r_evi`计算F1分数衡量验证性能。推理时，将候选列表分为大小为k的滑动窗口（k=7），由EVIAGENT联合验证：对子问题仅保留被标记为证据的页面，丢弃其余；对根问题构建优先级序列，证据页面在前，非证据页面按原始相似度顺序在后，作为安全后缀。
  - **记忆引导迭代生成**：首先，依次用子问题及其证据页面调用EVIAGENT，将子问题、推理轨迹和中间答案累积到显式记忆`M`中。然后，对根问题，模型输入根查询、记忆和当前证据块，若输出为`NOT_ANSWERABLE`，则追加当前推理轨迹到记忆并获取下一块，重复直至产生有效答案或序列耗尽。
- **算法流程**（文字说明）：
  1. 输入文档页面序列`D`和用户问题`q`。
  2. 通过LLM将`q`分解为子问题集合`Q={q1,...,qm}`（m≤5）。
  3. 对`q`和每个`qi`，用多模态检索器从`D`中检索Top-K候选页面`T_q`和`T_qi`。
  4. 对每个候选列表，分割成块，用训练好的EVIAGENT逐块验证，输出证据决策和优先级序列。
  5. 先顺序处理子问题：用EVIAGENT对`qi`+`E(qi)`生成`(τ_i, y_i, a_i)`，更新记忆`M`。
  6. 迭代处理根问题：用EVIAGENT对`q`+`M`+当前块`bk_t`生成输出，若为NOT_ANSWERABLE则更新记忆并取下一块，否则返回最终答案。

## 3. 实验设计：数据集、基准、对比方法
- **数据集**：四个基准：
  - **PaperTab**、**FetaTab**：科学和Wikipedia风格表格上的问答（平均页数10.7、16.3）。
  - **MMLongBench**：跨页检索、视觉定位和多跳推理（平均页数47.8）。
  - **LongDocURL**：全面多模态长文档理解（平均页数89.0）。
- **基准设置**：按原始评估协议，对MMLongBench和LongDocURL使用准确率和规则字符串匹配；对PaperTab和FetaTab采用LLM-as-judge（Qwen3.5-27B）评估二进制准确率。额外报告Recall@K、NDCG@K、MRR@K等检索指标。
- **对比方法**：
  - M3DocRAG (7B)、MDocAgent (7B)、MoLoRAG+ (7B)、ALDEN (7B)、URaG (7B)、Doc-V* (7B)。均为开源或可复现的强基线。
- **消融实验**：验证三个关键模块的贡献：
  - w/o Evidence-Aware Verification (跳过Step3)
  - w/o Hierarchical Decomposition (跳过Step1)
  - w/o Iterative Reasoning (单次传递Top-5)
- **敏感度分析**：交叉组合不同生成骨干（Qwen2.5-VL-7B、Qwen3-VL-8B）和检索器（ColQwen2.5、Ops-ColQwen3-4B）。还分析了块大小k和训练前后EVIAGENT的检索效果。

## 4. 资源与算力
- **训练EVIAGENT**：基础骨干为Qwen3-VL-8B-Instruct，在8× NVIDIA A100 GPU集群上训练。训练数据来自6个多页数据集（DUDE、MP-DocVQA、MultiHiertt、pdfQA、SciEGQA、SlideVQA），每个数据集均匀采样1000个实例。
- **推理阶段**：层次分解、多块验证、记忆引导生成需要多次顺序LLM/VLM调用，计算开销较大。论文明确指出了这一局限性。
- **未明确说明**：训练具体轮数、时长、总参数量等细节未给出。

## 5. 实验数量与充分性
- **实验数量**：
  - 主要结果表（Table 2）在4个数据集上对比7个基线。
  - 消融实验（Table 3）测试3个变体，每个变体在4个数据集上的结果，共12项。
  - 骨干/检索器敏感度分析（Table 4）：4种组合×4数据集=16项。
  - 检索性能验证（Table 5）：3种Top-K×2种方法×2个数据集=12项（含训练前后对比）。
  - 块大小实验（Figure 2）：在MMLongBench上测试不同k值的检索指标。
- **充分性评价**：实验设计较为全面：
  - 覆盖表格类（PaperTab、FetaTab）和长文档多模态类（MMLongBench、LongDocURL）两个主要场景。
  - 消融实验验证每个模块的必要性。
  - 敏感度分析证明方法不依赖特定骨干/检索器，优势来自架构创新。
  - 检索指标直接验证EVIAGENT的验证效果（对比训练前和训练后）。
  - 评估公平性：所有基线在相同生成骨干或检索器下比较（如Table 4中与MoLoRAG+使用相同配置）。但个别基线结果可能来自原论文报告，实验环境（如GPU、显存）可能不完全一致，但作者已尽量控制变量。

## 6. 论文的主要结论与发现
- **主要结论**：HIEVI-RAG在四个基准上均显著超越所有开源基线，平均准确率超过最强基线8.05%。
  - 相比M3DocRAG（7B）提升约15.5个百分点（平均）。
  - 相比MoLoRAG+（7B）提升约12.6个百分点。
- **关键发现**：
  - 层次化问题分解降低了多跳查询的检索难度，避免了证据遗漏（消融实验下降2.7%）。
  - 细粒度证据验证有效过滤了主题相关但无答案的干扰页面，提升检索NDCG@1达7.9%（MMLongBench）和5.4%（LongDocURL）。
  - 记忆引导迭代生成作为安全网，防止局部窗口内的信息缺失（消融实验下降1.9%）。
  - 各模块协同作用：单一模块去除均导致性能下降，验证了整体设计的必要性。

## 7. 优点
- **方法论亮点**：
  - **层次化分解**：将复杂问题拆解为原子子问题，降低检索难度，同时通过子问题的中间结果辅助根问题，形成知识蒸馏。
  - **细粒度验证**：训练EVIAGENT进行跨页面推理，不依赖简单相似度，而是基于证据的联合判断，显著提升检索精度。
  - **记忆引导迭代**：动态更新显式记忆，避免单次传递的级联错误；同时利用“NOT_ANSWERABLE”机制实现自适应停止，提升效率。
  - **模块化设计**：各阶段协同但可独立替换（如不同骨干/检索器），泛化性强。
- **实验设计亮点**：
  - 多维度消融实验（模块、骨干、检索器、块大小）。
  - 直接对比训练前后的EVIAGENT检索效果，证明强化学习的必要性。
  - 在多个基准上一致表现，统计显著性强。

## 8. 不足与局限
- **计算开销**：多阶段顺序调用LLM/VLM增加了推理延迟和成本，不适合实时或低资源场景。
- **依赖中间推理质量**：记忆引导机制依赖子问题推理轨迹的准确性；若初始子问题产生严重幻觉，错误可能级联到根答案。
- **域外泛化有限**：仅评估了数字原生的电子文档，对退化扫描件、手写内容、自然场景文本的零样本泛化能力未验证。
- **实验覆盖**：
  - 所有基线参数量在7B-8B，未评估更大模型（如72B+）或更强检索器的表现。
  - 块大小k=7的结论可能依赖于数据集特性，未在其他场景下充分验证。
- **报告完整性**：训练细节（轮数、学习率、GRPO超参数）未完全公开，影响可复现性。
- **潜在偏差**：LLM-as-judge评估（PaperTab/FetaTab）可能引入评判偏见；训练数据取自6个开源数据集，未考虑数据多样性（如非英文文档）。

（完）
